# 감정의 인상 — 하드웨어 구성

> 작성 2026-09-07 · 설계 원본 `Project_감정의인상.md` · 소프트웨어 쪽은 `PIPELINE.md`
> 예산상 이 구성은 전부 **임차**다 (자산취득비 집행 불가 → 4장)

---

## 1. 전체 구성 — 3계층

장비는 세 덩어리뿐이다. 카메라 한 대, 워크스테이션 한 대, 화면 한 장.

```
        관객
         │  0.5~1m 근접, 화면을 정면으로 마주 봄
         ▼
┌─────────────────────────────────────────────────────┐
│ 입력   깊이 센싱 카메라 1대                          │
│        RGB 4K 30fps + 깊이 + 32관절 스켈레톤         │
│                                                     │
│        ├── 전체 프레임 ──────→ 실루엣 추출 (7단계)   │
│        ├── 얼굴 영역 디지털 크롭 → AU 분석 (3단계)   │
│        └── 스켈레톤 관절 변화 → 움직임 에너지        │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│ 연산   GPU 탑재 로컬 워크스테이션 1대                │
│        py-feat 추론 · 판정 엔진 · TouchDesigner 렌더 │
│        외부 서버 통신 없음 · 얼굴 원본은 파기        │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│ 출력   파인피치 LED 디스플레이                       │
│        2.2m(H) × 2.93m(W) · 4:3 · P1.2~1.5mm         │
└─────────────────────────────────────────────────────┘
```

**카메라 한 대가 세 가지 일을 동시에 한다.** 이게 구성의 핵심이다.
4K 원본에서 얼굴 영역만 디지털로 크롭하면, 전신을 담는 넓은 화각과
AU 분석에 필요한 얼굴 해상도가 상충하지 않는다. 카메라를 추가하지 않고
"몰입 화면용"과 "표정 분석용"을 함께 커버한다.

---

## 2. 장비별 사양과 근거

### 출력 — 파인피치 LED 디스플레이

| 항목 | 값 | 근거 |
|---|---|---|
| 크기 | **2.2m(H) × 2.93m(W)** | 아래 |
| 비율 | 4:3 | 중앙 전신 실루엣 + 좌우 원소 비주얼 여백 |
| 화소피치 | **P1.2~1.5mm** | 관객이 0.5~1m에서 보므로 픽셀이 보이면 안 됨 |
| 방식 | 일반 LED (비투명) | 2026-08-14 확정 |

**높이 2.2m가 나온 계산** — 20대 평균 신장(남 174.8cm / 여 163.6cm)에
선 채로 손을 뻗어 닿는 최대 높이(키 × 1.23~1.25)를 적용하면 남 약
2.17m, 여 약 2.03m다. 2.2m는 그 바로 위이며, 더 높이면 **손이 닿지 않는
죽은 공간**이 생긴다. 폭은 여기에 4:3을 적용해 2.93m.

관객의 몸이 실제로 닿는 범위를 화면의 물리적 상한으로 삼은 것이다.

**왜 프로젝터가 아닌가** — 주변 조명에 약하고, 정렬이 복잡하며, "거울
앞에 선다"는 즉각성이 나오지 않는다. LCD는 이 작품이 요구하는 스케일감에
비해 화면이 너무 작아 제외했다.

**상위 옵션(보류)** — 예산이 크게 확보되면 투명 LED로 전환 검토 가능.
BUCK · Afterpay Arena 레퍼런스급. 현 시점 기본안은 일반 LED다.

### 입력 — 깊이 센싱 카메라

| 항목 | 값 | 비고 |
|---|---|---|
| RGB | **3840×2160 @ 30fps** | 4096×3072(4:3) 모드는 15fps라 **피할 것** |
| 수평화각 | 약 90° | 관객이 2m 앞에 서면 폭 약 4m 커버 |
| 스켈레톤 | 32관절 | arousal의 움직임 재료 (`motion_energy_from_joints`) |
| 대수 | **1대** | 전신 + 얼굴 크롭을 한 프레임에서 |

기준 장비는 Azure Kinect DK이고 TouchDesigner 실루엣 추출 테스트를
마쳤다. **다만 단종 문제가 있다 → 5장.**

### 연산 — GPU 탑재 로컬 워크스테이션

**사양 미정.** 이 문서에서 유일하게 비어 있는 칸이고, 파이프라인의
1초 예산이 여기 걸려 있다 → 3장.

확정된 것은 원칙뿐이다. **외부 서버를 쓰지 않는다.** 감정 인식·문구
생성·실시간 렌더링을 전부 현장에서 처리한다. 얼굴 원본은 이 기계 밖으로
나가지 않고 9단계에서 파기된다.

---

## 3. 파이프라인과의 접점 — 3초 예산은 누가 쓰는가

1~7단계 연산은 3초 이내여야 한다. 그 예산을 실측·추정으로 나눠 보면
**한 곳이 사실상 전부를 쓴다.**

| 단계 | 처리 | 소요 | 근거 |
|---|---|---|---|
| 1·2 감지·캡처 | 프레임 취득 | 약 33ms | 30fps = 프레임당 33.3ms |
| **3 분석** | **py-feat 추론** | **미측정 — 목표 1초 이내** | **← 예산의 주인** |
| 4·5·6 변환·판정·문구 | 판정 엔진 | **0.02ms** | 실측 (아래) |
| **7 렌더** | **TouchDesigner** | **미측정** | **← 나머지 예산** |

**판정 엔진 실측 (2026-09-07, CPU)**

```
어댑터 (py-feat 행 → 신호)            중앙 0.0104ms   p99 0.0281ms
엔진 4~6단계 (변환·판정·문구)          중앙 0.0071ms   p99 0.0210ms
전 구간 (어댑터+엔진+렌더사양)          중앙 0.0215ms   p99 0.0437ms

10,000회 연속 0.23초 → 초당 43,494회
```

엔진이 쓰는 건 3초 예산의 **0.0007%**다. GPU도 필요 없다. 설계할 때
연산 부담을 걱정했던 4~6단계는 사실상 공짜로 판명됐다.

**따라서 워크스테이션 사양은 두 가지만 결정한다** — py-feat 추론
속도와 TouchDesigner 실시간 렌더. 둘 다 GPU가 결정한다.

---

## 4. 조달 — 전부 임차

공모 예산 규칙상 **자산취득비(카메라·GPU 등 장비 구매)는 집행 불가**다.
다만 임차료 항목으로 장비·전자기기·소프트웨어 대여는 가능하다.

| 목/세목 | 용도 | 산출근거 | 지원금 | 자부담 | 합계 |
|---|---|---|---|---|---|
| 운영비/임차료/기타임차료(장비) | LED 디스플레이 · 센싱 카메라 · 렌더링 서버 | 3개월 임차 1식 | 12,000,000 | 3,000,000 | 15,000,000 |

3종을 묶어 1식으로 편성했다. 시스템 개발은 별도로 대행용역비
(18,000,000)에 있다.

> 임차라는 조건이 5장의 리스크를 완화한다 — 단종된 장비도 대여 시장에는
> 남아 있고, 구매가 아니므로 대체 장비로 갈아타기도 쉽다.

---

## 5. 리스크 — 기준 카메라가 단종됐다

**Azure Kinect DK는 2023년 8월 단종 발표, 10월 생산 종료됐다.**
설계 문서가 이 장비를 기준으로 쓰였고 예산에도 반영돼 있으므로 명시해
둔다. 신품 조달은 불가능하다고 보는 게 안전하다.

**후속 장비가 있다.** Microsoft가 iToF 기술을 Orbbec에 이관했고,
**Orbbec Femto Bolt**가 공식 후속이다.

| 항목 | Azure Kinect DK | Femto Bolt |
|---|---|---|
| RGB | 3840×2160 @30fps | **동일** |
| 깊이 | MS iToF | **동일 기술** |
| 화각 | 수평 약 90° | 약간 좁음 |
| Sensor SDK | K4A | **K4A Wrapper로 호환** |
| Body Tracking | 32관절 | **동일 SDK가 수정 없이 동작, 출력 형식 동일** |
| 조달 | 단종 | 현행 생산 |

**우리 코드에 미치는 영향: 없다.** 32관절 출력 형식이 같으므로
`motion_energy_from_joints`가 그대로 돈다. 화각이 약간 좁은 것만
설치 거리에서 흡수하면 된다(관객 2m 기준 커버 폭이 조금 줄어듦 —
목업 테스트에서 확인).

**⚠️ 확인 필요** — 위 비교는 제조사 문서와 2차 자료 기준이다. 실제
임차 견적을 낼 때 국내 대여 가능 여부와 화각 실측치를 확인해야 한다.
Femto Mega는 Jetson을 내장해 깊이 처리를 자체 수행하므로, 워크스테이션
부하를 덜어야 할 경우의 대안으로 함께 검토할 만하다.

---

## 6. 미확정 목록

| # | 항목 | 무엇에 막혀 있나 | 우선순위 |
|---|---|---|---|
| 1 | **워크스테이션 GPU 사양** | py-feat 추론 + TD 렌더 실측이 있어야 정할 수 있다 (→ `PIPELINE.md` C-3b) | **높음** |
| 2 | 카메라 기종 확정 | Femto Bolt 국내 임차 가능 여부·견적 | 높음 |
| 3 | LED 실제 크기 | 설치 구역 치수 미확정. 2.2×2.93m는 시안·데모 기준값 | 설치 확정 시 |
| 4 | 카메라 설치 높이·기울기 | 목업 테스트 필요 | 설치 확정 시 |
| 5 | 얼굴 크롭 해상도 | 목업에서 AU 인식이 성립하는 최소 픽셀 확인 | C-3와 함께 |

**1번이 임계 경로다.** 그리고 1번은 `PIPELINE.md`의 C-3b(성능 실측)와
같은 문제다 — 소프트웨어 병목과 하드웨어 미확정 항목이 한 점에서
만난다. 실측 한 번이 둘 다 푼다.

---

## 7. 지금 있는 것 / 없는 것

| | 상태 |
|---|---|
| 깊이 센싱 카메라 | **보유** — TouchDesigner 실루엣 추출 테스트 완료 |
| TouchDesigner | **보유** |
| 판정 엔진 | **보유** — 구현·검증 완료, 0.02ms |
| py-feat 인식 계층 | 어댑터만 완료, 모델 실행 미검증 |
| GPU 워크스테이션 | 사양 미정 |
| LED 디스플레이 | 미조달 (임차 예정) |

데모 영상(→ `PIPELINE.md` F)에 LED는 필요 없다. 카메라 + 워크스테이션 +
모니터만 있으면 "사람이 서면 색이 맺히고 질문이 뜬다"는 흐름을 찍을 수
있다. **LED는 설치 단계의 장비이지 데모의 전제 조건이 아니다.**

---

## 출처

- Azure Kinect DK 스펙 —
  [Microsoft Learn](https://learn.microsoft.com/en-us/previous-versions/azure/kinect-dk/hardware-specification) ·
  [스펙 PDF](https://ifelldh.tec.mx/sites/g/files/vgjovo1101/files/Azure%20Kinect%20DK%20Specifications.pdf)
- 단종 —
  [Azure Kinect (Wikipedia)](https://en.wikipedia.org/wiki/Azure_Kinect) ·
  [Hackaday](https://hackaday.com/2023/08/26/microsoft-discontinues-kinect-again/)
- 후속 장비 —
  [Femto Bolt 대 Azure Kinect DK 비교](https://www.orbbec.com/documentation/comparison-with-azure-kinect-dk/) ·
  [OrbbecSDK-K4A-Wrapper](https://github.com/orbbec/OrbbecSDK-K4A-Wrapper)
- 엔진 지연 — 이 저장소에서 직접 실측 (2026-09-07, CPU)
