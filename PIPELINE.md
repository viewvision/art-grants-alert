# 감정의 인상 — 제작 파이프라인

> 작성 2026-09-07 · 공모 결과와 무관하게 진행하는 자체 개발 로드맵
> 설계 원본: `Project_감정의인상.md` · 판정 엔진 구현: `impression_engine/`

---

## 0. 이 문서의 전제

지원사업 미선정 이후 정리한 원칙이 하나 있다. **지원금 없이도 엔진을
실물로 만들어 두는 것이 다음 공모의 최대 무기**라는 것이다.
(→ `선정기업_분석_2026_오픈이노베이션2차.md` 패턴 2)

선정 기업들은 전부 **이미 굴러가는 것**을 들고 왔다. 우리가 제출한 건
계획이었다. 이 파이프라인의 목표는 그 격차를 없애는 것이다.

---

## 1. 전체 구조 — 9단계 파이프라인

```
 [1]감지 → [2]캡처 → [3]분석 → [4]변환 → [5]판정 → [6]문구 → [7]렌더 → [8]재캡처 → [9]저장
   보유      보유      개발      완료      완료      부분      보유      개발       개발
                              └───── impression_engine ─────┘
                       ← 연산 3초 이내 ─────────────────────→
```

| 단계 | 처리 | 산출물 | 현재 상태 |
|---|---|---|---|
| 1 감지 | 화면 앞 관객 진입 감지 | 관객 유무·거리 | **보유** (Kinect DK 테스트 완료) |
| 2 캡처 | 전신 + 얼굴 영역 동시 취득 | RGB 4K 프레임 | **보유** |
| 3 분석 | 얼굴 근육 단위(AU) → 감정 원점수 | 6개 감정값 + AU6/12 + arousal | **미착수** ← 최우선 |
| 4 변환 | 6감정 → 4방향 비중·톤·강도 | 4방향 비율(합 100%) | **완료** `directions.py` |
| 5 판정 | 4단계 알고리즘 → 슬롯 결정 | 24개 중 1개 | **완료** `judgment.py` |
| 6 문구 | 슬롯값 조건 → 질문 1문장 | 표시 문구 | **부분** (1·3계층 완료, 2계층 모델 미정) |
| 7 렌더 | 색·질감·움직임으로 그리기 | 실시간 화면 | **보유** (TouchDesigner) |
| 8 재캡처 | 문구 본 직후 반응 재취득 | 갱신 파라미터 | **미착수** |
| 9 저장 | 얼굴 원본 파기, 익명 수치만 | 감정 파라미터 로그 | **미착수** |

**핵심**: 4·5단계는 코드로 완성됐고 테스트를 통과한다. 병목은 3단계다.

---

## 2. 지금 만들어진 것 — `impression_engine/`

```
impression_engine/
├── signals.py      입력 신호 정의 (에크만 6 + arousal + AU6/12)
├── arousal.py      강도 산출 (표정 강도 + 움직임 에너지 가중합)
├── directions.py   4방향 변환 + 방향별 valence 계산
├── judgment.py     4단계 판정 알고리즘 → 24개 슬롯
├── palette.py      슬롯 → 색·비중·배경색
├── phrases.py      1계층 기준 문구 + 3계층 규칙 필터
├── engine.py       통합 API (process 함수 하나)
└── cli.py          데모 실행기
tests/test_engine.py  검증 (pytest 불필요)
```

### 실행

```bash
python3 -m impression_engine.cli             # 대표 사례 8개 판정 결과
python3 -m impression_engine.cli --json      # 렌더 사양 JSON
python3 -m impression_engine.cli --coverage  # 문구 집필 진행률
python3 tests/test_engine.py                 # 전체 검증
```

### 검증 결과 (2026-09-07)

- 입력 조합 **18,750개 전수 스윕** — 예외·규칙 위반 0건
- 설계상 24개 슬롯 **전부 도달 확인**
- 비율 합계가 항상 정확히 1.0
- 생성 실패·필터 미통과 시 1계층 폴백 정상 동작

### 사용 예

```python
from impression_engine import EmotionSignal, process

result = process(EmotionSignal(sadness=0.9, disgust=0.3, arousal=0.75))
print(result.judgment.slot_id)   # water_high
print(result.palette.as_dict())  # 색·비중·배경
print(result.to_render_spec())   # TouchDesigner로 넘길 JSON
```

---

## 3. 구현하면서 드러난 설계 결함 3건

문서로만 있을 때는 안 보이다가, 코드로 만들자 바로 드러난 것들이다.
**전부 수정 완료**했고 근거를 남긴다.

### 결함 1 — 흙 계산식이 단일 감정을 삼켰다

각 방향이 감정 2개의 합(0~2)이라 2로 나눠 평균을 냈더니, **분노 0.9인
사람이 흙(평온) 1위로 판정**됐다. 짝 감정(기쁨)이 0이라 활성도가 절반으로
깎였기 때문이다.

→ 합을 그대로 쓰도록 수정. 분노 0.9 → 불 90%로 정상화.

### 결함 2 — 2위 원소가 0%인데도 같은 강도로 칠해졌다

"한 화면에는 두 원소만"을 구현했더니, 물 100% / 불 0%인 화면에서
**비중 0%인 불이 1위와 동등한 강도로** 나갔다. 판정 결과가 시각적으로
왜곡되는 문제다.

→ 팔레트가 색과 함께 **비중(weight)** 을 넘기도록 수정. 2위 비중이
2% 미만이면 아예 화면에 올리지 않는다.

### 결함 3 — 공기의 '희망'은 AU 없이는 구조적으로 도달 불가

공기 valence는 `공포 ↔ (놀람 중 웃음근육 동반분)` 대결로 계산한다.
그런데 웃음근육(AU6·12) 입력이 없으면 양수 쪽 재료가 0이 되어,
**공기는 항상 불안으로만 판정된다.** 슬롯 6개 중 희망 계열 3개가
영구 미도달 상태가 된다.

→ 엔진 입력에 `smile_au`를 명시적으로 두고, 인식 계층이 반드시 공급하도록
인터페이스에 못 박음. 3단계 모델 선정 시 **AU6·AU12를 함께 출력하는
모델**이어야 한다는 요구사항이 여기서 확정됐다.

---

## 4. 남은 스펙 공백 — 2건 (1건 해결)

### ✅ 해결 — arousal 산출 방식 (2026-09-07 확정)

**arousal = 0.5 × 표정 강도 + 0.5 × 움직임 에너지**

구현: `impression_engine/arousal.py` · 근거 전문: `Project_감정의인상.md`
"arousal(강도) 산출 방식"

6개 감정 확률에서 유도하면 방향축과 재료가 겹쳐 축이 무너진다. 전수 검증
결과 감정 총합으로 유도하면 24개 중 6개, 최대 감정값으로 유도하면 3개가
도달 불가능해진다. 특히 **흙·고** — "고요해 보이지만 속이 들끓는 사람"이
사라진다.

표정 강도만 써도 무표정한 사람의 강도가 0이 되어 흙·고가 여전히 죽고,
움직임만 쓰면 격한 표정으로 가만히 선 사람이 낮게 잡힌다. 두 신호를 섞어
서로의 결함을 메운다.

확률합(soft OR)도 비교했으나 격자의 70%가 '고'로 쏠려 강도축이 무너진다.
가중합은 저/중/고를 24:52:24로 고르게 쓴다.

> **이전 기술 오류 정정** — 이 문단은 원래 "흙·중, 흙·고 둘 다 영구
> 미도달"이라고 적혀 있었다. 실제로 전수 검증해 보니 죽는 흙 슬롯은
> `earth_high` 하나이고, 대신 `water_low` `fire_joy_low` 등 다른 슬롯이
> 함께 죽는다. 결론(감정에서 유도하면 안 된다)은 같지만 근거 수치가
> 틀렸으므로 바로잡는다.

### 공백 1 — 물의 고정 valence 값

문서상 "-0.3 ~ -0.6" 범위만 있다. 현재 중앙값 **-0.45**를 썼다.
물은 톤 분기가 없어 슬롯 선택에는 영향이 없지만, 렌더 단계에서 밝기를
정할 때 쓰이므로 확정이 필요하다.

### 공백 2 — 배경색 실제 HEX

"1위의 반대 온도"라는 규칙은 확정됐지만 실제 색값은 미확정이다.
현재 대비 테스트에서 쓴 잠정값을 넣어뒀다.

```
차가운 배경(불·흙 1위)  #BFE3F7 → #78C4F0
따뜻한 배경(물·공기 1위) #FFE0C4 → #FFB37A
```

---

## 5. 다음 단계 — 우선순위

### 1순위 · 3단계 감정 인식 모듈 (엔진 완성의 유일한 병목)

지금 엔진은 감정 수치만 넣으면 완전히 돌아간다. 그 수치를 만드는
계층만 붙이면 **캡처→화면까지 전 구간이 연결된다.**

요구사항이 구현 과정에서 전부 확정됐다:

- 에크만 6대 감정 출력 (각 0~1)
- **AU6·AU12 개별 출력** ← 결함 3. 공기의 희망/불안 분기에 필수
- **AU intensity 원값** ← arousal 결정. 강도의 표정 쪽 재료
- 로컬 구동, 외부 서버 통신 없음
- 목표 지연 1초 이내 (전체 3초 예산 중)

여기에 Kinect 스켈레톤에서 뽑는 움직임 에너지를 더하면 arousal이 완성된다
(`arousal.motion_energy_from_joints`가 이미 구현돼 있다).

후보 검토 대상: `py-feat`, `OpenFace 2.0`, `libreface` 등 AU 출력이
가능한 오픈소스. **선정 기준은 정확도가 아니라 위 세 항목을 함께
내주는가**이다.

### 2순위 · 1계층 기준 문구 나머지 집필

현재 한국어 8/24, 영어 1/24. `--coverage`로 남은 슬롯이 정확히 나온다.
이건 작가 작업이고 도구는 준비됐다.

### 3순위 · TouchDesigner 연결

엔진이 내놓는 `to_render_spec()` JSON을 TD가 받아 그리는 구간.
색·비중·배경이 전부 사양에 들어 있으므로, TD 쪽은 이 JSON만 파싱하면 된다.

### 4순위 · 2계층 로컬 생성 모델

`PhraseGenerator` 인터페이스가 이미 정의돼 있어 나중에 끼워 넣기만 하면
된다. **없어도 1계층으로 작품은 완성된다** — 폴백이 구조적으로 보장돼
있으므로 이 단계는 급하지 않다.

### 5순위 · 8·9단계 (재캡처·저장)

작품의 완결성에는 필요하지만 데모 확보에는 필수가 아니다.

---

## 6. 데모 목표

다음 공모까지 확보해야 할 것은 완성품이 아니라 **"이만큼 돌아갑니다"**를
보여주는 30초짜리 영상이다.

```
사람이 화면 앞에 선다
  → 실루엣이 잡힌다                    [보유]
  → 표정이 감정 수치로 변환된다         [1순위]
  → 색이 맺힌다                        [엔진 완료 + TD]
  → 질문이 뜬다                        [엔진 완료]
```

이 흐름이 한 번이라도 실제로 돌아가면, 다음 신청서의
"기술력 및 BM 보유 여부"(협업계획 50점의 핵심 지표) 항목이
**계획에서 보유로 바뀐다.** 그게 이번 탈락의 가장 유력한 원인이었다.

---

## 7. 엔진에 붙일 제품명 — 미정

`선정기업_분석` 실행항목 3번. '감정의 인상'은 작품명이지 제품명이 아니다.
팀펄의 '세파퓨처리즘', 포밍부스의 '캔버스 파크'가 하는 역할을 할
이름이 필요하다. 엔진 패키지명(`impression_engine`)은 임시다.
