# 3단계 감정 인식 모듈 — 후보 비교 (2026-09-07)

> 파이프라인 9단계 중 유일한 병목. → `PIPELINE.md` 1순위
> 이 문서의 결론은 `Project_감정의인상.md`에 확정 기록으로 옮긴다.

---

## 0. 선정 기준 — 정확도가 아니다

엔진(`impression_engine/`)이 이미 완성돼 있으므로, 모델에 요구되는 건
성능이 아니라 **출력 항목**이다. 하나라도 빠지면 슬롯이 죽는다.

| # | 요구 출력 | 없으면 무슨 일이 | 확정된 곳 |
|---|---|---|---|
| 1 | 에크만 6대 감정 (각 0~1) | 4방향 비중을 못 만든다 | 2026-08-12 |
| 2 | **AU6·AU12 개별값** | 공기가 항상 불안으로만 판정 — 희망 3슬롯 사망 | 설계 결함 3 |
| 3 | **AU intensity 원값** | arousal의 표정 쪽 재료가 없다 | 2026-09-07 arousal 결정 |
| 4 | 로컬 구동 · 1초 이내 | 3초 예산 초과 / 외부 전송은 설계 위반 | 2026-08-11 |
| 5 | **상업 이용 가능 라이선스** | 설치 자체가 불가능 | ← 이번 검토에서 추가 |

**5번을 기준에 추가한다.** 이전까지 놓치고 있던 항목이다. VIEWVISION은
영리법인이고, 목표 설치 공간(LG유플러스 「일상비일상의틈」)은 브랜드
상업공간이다. 연구용 라이선스 모델을 쓰면 **작품을 설치하는 순간 위반**이
된다. 그리고 이 기준 하나가 기존 후보 3개 중 2개를 탈락시킨다.

---

## 1. 결론

**py-feat 2.1.2 채택 — 단, v1 경로로.** 5개 기준을 모두 충족하는 유일한
후보다.

기존에 검토 대상으로 적어 두었던 OpenFace 2.0과 LibreFace는 **둘 다
비상업 라이선스라 탈락**한다. 성능 문제가 아니라 쓸 수 없는 문제다.
py-feat도 **v2 멀티태스크 가중치는 `research-only`라 쓸 수 없고**,
라이선스가 문서화된 v1 경로(`xgb_au` / `svm_emo`)를 쓴다. AU 20종이
v2와 동일해 설계에는 지장이 없다.

---

## 2. 비교표

| 후보 | 라이선스 | 상업 | 6감정 | AU6·12 | AU 강도 | arousal | 판정 |
|---|---|---|---|---|---|---|---|
| **py-feat 2.1.2 · v1** | MIT + 모델별 명시 | ✅ | ✅ 7종 | ✅ | ✅ 20종 | ❌ | **채택** |
| ~~py-feat 2.1.2 · v2~~ | ~~research-only~~ | ❌ | ✅ 7종 | ✅ | ✅ 20종 | ✅ | **탈락** |
| OpenFace 2.0 | CMU 비상업 | ❌ | 간접 | ✅ | ✅ | ❌ | **탈락** |
| LibreFace | USC research | ❌ | ✅ | DISFA 12종 | ✅ | ❌ | **탈락** |
| MediaPipe Face Landmarker | Apache 2.0 | ✅ | ❌ | 유사값 | 블렌드셰이프 | ❌ | 보조 |
| HSEmotion (ONNX) | Apache 2.0 | ✅ | ✅ | ❌ | ❌ | ❌ | 폴백 |

---

## 3. 후보별 검토

### ✅ py-feat 2.1.2 — 채택

소스를 직접 받아 출력 스키마를 확인했다(추정이 아니라 코드 확인).

**출력 항목** (`feat/multitask/__init__.py`, `feat/multitask/model_v2.py`)

```
AU        20종 연속값 — AU01 02 04 05 06 07 09 10 11 12 14 15 17 20 23 24 25 26 28 43
          → AU06·AU12 둘 다 포함 ✅  (v2.4에서 빠진 건 AU16·18·27·45)
감정      7종 — Neutral, Happy, Sad, Surprise, Fear, Disgust, Anger
V/A       valence · arousal 연속 회귀 [-1, 1]   ← 예상 못 한 수확
메시      MediaPipe 478포인트
```

**라이선스** — 툴박스 본체 MIT. 동봉 LICENSE를 끝까지 읽은 결과 비상업
제한이 걸린 건 **ArcFace ResNet50 가중치 하나뿐**이다. InsightFace의
비상업 연구 조항과 WebFace600K 학습 데이터 제한을 물려받는다.

> **그런데 ArcFace는 신원 식별(identity) 모델이다.** 본 작품은 얼굴을
> 식별하지 않는 것이 핵심 원칙이고 9단계에서 원본을 파기한다. 애초에
> 쓸 일이 없는 모듈이므로, **비활성화하면 상업 이용에 걸림돌이 없다.**
> 우연이 아니라 설계 원칙이 라이선스 리스크를 미리 없앤 경우다.

**🔴 v2 가중치 — 비상업 확정 (2026-09-07, 모델 카드 원문 확인)**

모델 카드에 `License: research-only`로 명시돼 있다.

> "Research / non-commercial use only. Trained on datasets (AffectNet,
> DISFA+, RAF-DB, Aff-Wild2, AFEW-VA, **BP4D**, ETH-XGaze, EYEDIAP, etc.)
> whose licenses restrict use to academic research."

예상대로 학습 데이터셋의 제한이 가중치에 따라붙었다. **v2 탈락, v1 확정.**

부수 효과로 **논점 1이 닫힌다** — v2를 못 쓰니 네이티브 arousal도 못
쓴다. 9/7에 확정한 `0.5 × AU강도합 + 0.5 × 움직임`이 변경 없이 그대로
간다.

### 🔴 모델 카드에서 나온 별도 문제 — AU06 신뢰도

라이선스보다 설계에 더 직접적인 발견이 하나 있다.

> "AU20 is effectively non-functional (per-AU F1 0.057 against a 0.682
> macro). **AU15 (0.479) and AU06 (0.526)** are also well below the macro
> average. ... **treat AU15/AU06 with caution.**"

**AU06은 공기의 희망/불안을 가르는 두 근육 중 하나다.** macro 0.682인데
AU06이 0.526이면, 우리 설계에서 가장 예민한 분기점이 모델의 최약점 위에
놓여 있다는 뜻이다. AU12는 cross-tool common-8 세트(macro-F1 0.774)에
포함된 신뢰 구간이다.

기존 어댑터는 `mean(AU06, AU12)` — 신뢰도 낮은 신호에 동등한 가중치를
주고 있었다. 그러면 AU06의 오류가 그대로 슬롯 판정으로 넘어간다.

| AU06 오류 | mean일 때 | 결과 |
|---|---|---|
| 헛 반응 (AU06 0.9 / AU12 0.1) | 0.5 | 희망 슬롯이 잘못 뜬다 |
| 놓침 (AU06 0.1 / AU12 0.9) | 0.5 | 진짜 웃음이 절반으로 깎여 불안으로 떨어진다 |

→ **기본값을 `0.75 × AU12 + 0.25 × AU06`으로 변경.** 오차 기여가 1/4로
줄고, 위 두 경우가 0.3 / 0.7로 갈라진다.

그리고 이전에 넣어 둔 `strict` 모드(둘 중 작은 값, 뒤셴 판정)는 **약한
쪽이 게이트가 되므로 이 모델에서는 더 나쁘다.** 옵션으로는 남기되
비권장으로 표시했다.

> ⚠️ 위 수치는 v2 모델 카드 기준이고 실제로는 v1을 쓴다. v1의 AU06
> 신뢰도는 실측(C-3a)에서 확인해 재조정해야 한다. 다만 AU06(볼 올림)이
> 원래 검출이 어려운 AU라는 사실은 경로와 무관하므로 보수적으로 잡아
> 둔다.

<details>
<summary>확인 전 조사 기록 (2026-09-07)</summary>

배포 패키지와 GitHub main 브랜치의 LICENSE를 모두 확인했다. 열거된 모델은
RetinaFace · FaceBoxes · MTCNN · Img2Pose · Mobilenet · MobileFaceNet ·
PFLD · Residual Masking Network · Facenet · ArcFace **10종뿐이고,
v2 멀티태스크 모델(`py-feat/face_multitask_v2`) 항목은 아예 없다.**

README는 "Py-FEAT는 MIT지만 **사용하는 각 모델의 라이선스는 따로 지켜야
한다**"고 명시하면서 LICENSE 파일을 가리키는데, 정작 그 파일에 v2가
없다. 즉 **v2 가중치는 라이선스가 문서화되지 않은 상태**다.

> 조사 한계: 이 세션의 네트워크 정책이 huggingface.co를 차단해
> 모델 카드를 직접 볼 수 없었다. 확인은 외부에서 해야 한다.
> → https://huggingface.co/py-feat/face_multitask_v2

**확인해야 할 것 3가지**
1. 모델 카드에 라이선스 태그가 있는가
2. 학습 데이터셋이 무엇인가 — BP4D·DISFA 계열은 자체 EULA가 있고,
   그 제한이 가중치에 따라붙는 경우가 있다
3. 상업적 재배포·상업적 사용에 대한 별도 조항이 있는가

→ 세 가지 모두 위에서 확인됨. 추정이 맞았다.

</details>

### v1 경로 — 확정된 대안

v2가 막히면 v1(`xgb_au` / `svm_emo`)로 내려간다. 소스에서 확인한 결과
**우리 요구사항에는 지장이 없다.**

| 항목 | v1 출력 | 우리 요구 |
|---|---|---|
| AU | 20종 — AU01 02 04 05 **06** 07 09 10 11 **12** 14 15 17 20 23 24 25 26 28 43 | AU06·AU12 ✅ |
| 감정 | 7종 (anger disgust fear happiness sadness surprise neutral) | 에크만 6 ✅ |
| arousal | **없음** | — |

v1의 AU 목록은 v2와 완전히 동일하다. 유일한 차이는 arousal 회귀가 없다는
것인데, **9월 7일 arousal 결정이 애초에 네이티브 arousal을 전제하지
않았으므로 설계에 영향이 없다.** 표정 강도(AU 합) + 움직임 가중합이라는
현행 구조가 v1에서도 그대로 돌아간다.

> 결과적으로 arousal을 독립 신호로 설계한 결정이 **모델 선택의 자유도를
> 지켜 줬다.** 네이티브 arousal에 의존했다면 v2 라이선스가 막히는 순간
> 설계까지 되돌려야 했다.

### ❌ OpenFace 2.0 — 탈락 (비상업)

`TadasBaltrusaitis/OpenFace`. AU 성능은 업계 표준으로 통하지만, CMU가
**비상업 연구 목적으로만** 사용을 허가한다. 상업 라이선스는 별도 구매이며
공개된 가격은 연 **USD 10,000(Light) / 15,000(Landmark) / 18,000(Full)**
수준이다.

전체 사업 예산(5,600만 원)에 견주면 이 한 항목이 예산의 상당 부분을
먹는다. 게다가 매년 나가는 고정비다. **탈락.**

> 이름이 같은 `cmusatyalab/openface`(얼굴 인식 라이브러리)는 Apache 2.0
> 이지만 **AU를 내주지 않으므로** 우리 용도에는 무관하다. 혼동 주의.

### ❌ LibreFace — 탈락 (비상업)

`ihp-lab/LibreFace`. AU 강도 추정에서 DISFA 기준 PCC 0.63으로 OpenFace
2.0보다 7% 높다고 보고되며, AU 강도·감정 분류를 다 내주고 CPU 전용
버전도 있어 조건상 매우 잘 맞았다.

그러나 **USC research license** — 비상업이다. 성능이 아까워도 쓸 수 없다.
**탈락.**

### ◐ MediaPipe Face Landmarker — 보조

Apache 2.0, 상업 이용 자유. 매우 빠르고 CPU에서 잘 돈다. 52개
블렌드셰이프 중 `cheekSquint`(≈AU6)·`mouthSmile`(≈AU12)가 있어 웃음근육
신호는 얻을 수 있다.

**그러나 에크만 6대 감정을 내주지 않는다.** 블렌드셰이프 위에 분류기를
직접 학습시켜야 하는데, 그건 별도 프로젝트다. 단독 채택 불가.

다만 py-feat 2.x가 내부적으로 MediaPipe 478 메시를 쓰므로 사실상 함께
따라온다.

### ◐ HSEmotion — 폴백

`av-savchenko/hsemotion-onnx`. Apache 2.0, 학술·상업 모두 제한 없음.
ONNX라 매우 가볍고 빠르다.

**AU를 안 내준다.** 감정 분류만 한다. 따라서 단독으로는 요구 2·3번을
못 채운다. py-feat가 현장에서 너무 느릴 경우 **감정 분류만 이쪽으로
바꾸고 AU는 다른 데서 받는 하이브리드**의 폴백 카드로 남겨 둔다.

---

## 4. 채택에 따라오는 후속 논점

### 논점 1 — py-feat의 네이티브 arousal을 쓸 것인가 ★

py-feat v2가 **arousal을 연속값으로 직접 내준다.** 9월 7일 arousal
결정 당시에는 몰랐던 사실이라 재검토가 필요하다.

| 안 | 산출 | 장점 | 단점 |
|---|---|---|---|
| **현행** | 0.5×AU강도합 + 0.5×움직임 | 움직임이 섞여 방향축과 독립. 흙·고 도달 | 정규화 기준을 현장에서 실측해야 함 |
| **대안 A** | py-feat arousal 그대로 | 구현 0. 학습된 값이라 더 자연스러울 수 있음 | **표정에서만 나온 값** — 방향축과 재료가 겹쳐 독립 축이 아니게 됨. 무표정한 사람의 흙·고가 다시 죽을 위험 |
| **대안 B** | 0.5×py-feat arousal + 0.5×움직임 | 표정 쪽 재료를 원시 AU합 대신 학습된 값으로 교체. 정규화 기준 실측 부담이 사라짐 | py-feat arousal의 [-1,1] → [0,1] 사상 방식을 정해야 함 |

**대안 B가 유력하다.** 현행 구조(가중합·움직임 결합)를 그대로 두고
표정 쪽 재료만 더 나은 것으로 갈아 끼우는 것이라, 9/7 결정의 근거를
하나도 무너뜨리지 않는다. `ArousalTracker`의 `au_intensity_sum` 인자에
py-feat arousal을 정규화해 넣으면 코드 변경도 거의 없다.

**실측 후 결정한다** — 실제 관객 영상에서 두 값이 얼마나 다르게 나오는지
보고 정한다.

### 논점 2 — 감정 라벨 사상

py-feat는 7종(Neutral 포함), 우리 엔진은 6종을 받는다.

```
happiness → joy      sadness  → sadness   anger → anger
fear      → fear     disgust  → disgust   surprise → surprise
Neutral   → 버린다
```

Neutral을 버려도 정보 손실이 없다. 우리 설계에서 평온은 **잔여값(흙)**
으로 자동 계산되기 때문이다(`earth = 1 − 활성 감정 합`). 오히려 Neutral을
따로 받아 쓰면 흙 계산과 이중 계산이 된다.

### 논점 3 — 하드웨어

py-feat는 ConvNeXt-V2 백본이라 CPU만으로는 1초 예산이 빠듯할 수 있다.
GPU를 전제로 잡되, 현장 실측에서 미달이면 HSEmotion 하이브리드로 전환한다.

---

## 5. 진행 상황

| | 항목 | 상태 |
|---|---|---|
| 1 | 후보 5종 비교 · 라이선스 검토 | ✅ 완료 |
| 2 | v2 가중치 라이선스 확인 | 🔴 **미해결** — 문서화돼 있지 않음 |
| 3 | 실측 — 지연 · AU06/12 반응성 · arousal 분포 | ⛔ **이 환경에서 불가** |
| 4 | `EmotionSignal` 어댑터 | ✅ 완료 — `impression_engine/adapters.py` |
| 5 | arousal 표정 쪽 재료 확정 (논점 1) | ⏸ 3번 이후 |

### 설치는 확인됨, 가중치는 못 받음

py-feat 2.1.2 설치 자체는 성공했고 출력 스키마도 런타임에서 재확인했다.

```
$ python3 -c "from feat.multitask import AU_COLUMNS_V2, EMOTION_COLUMNS_V2, VA_COLUMNS_V2"
AU  : AU01 02 04 05 06 07 09 10 11 12 14 15 17 20 23 24 25 26 28 43   (20종)
감정: Neutral Happy Sad Surprise Fear Disgust Anger                    (7종)
V/A : valence arousal
```

그러나 `Detectorv2()` 생성 시 가중치 내려받기에서 막힌다.

```
ProxyError: 403 Forbidden      # huggingface.co, 게이트웨이 정책 차단
```

**실측(3번)은 외부 환경에서 해야 한다.** 로컬 작업 PC나 설치 현장 장비
에서 진행한다. 이 환경에서 할 수 있는 일(어댑터·검증)은 끝냈다.

### 어댑터에서 확인된 것 — 설계와 모델의 대응

py-feat 감정 출력은 Neutral 포함 7종에 대한 확률이라 합이 1이다.
Neutral을 버리고 6종만 넘기면

```
흙 = 1 − (물 + 불 + 공기) = 1 − 활성 감정 합 = Neutral
```

이 되어 **엔진이 계산한 흙 비중이 py-feat의 Neutral 확률과 정확히
일치한다.** 잔여값으로 평온을 정의한 2026-08-12 설계와 모델 출력이
우연히 맞아떨어지는 지점이며, Neutral을 따로 받아 쓰면 오히려 이중
계산이 된다. 검증에 포함해 두었다.

### 어댑터에서 드러난 잠정값 문제

`arousal.DEFAULT_AU_REFERENCE`는 **12.0**인데 이건 OpenFace 기준
(AU당 0~5 intensity)이었다. **py-feat의 AU 출력은 0~1 확률값**이라 20개를
다 더해도 격한 표정에서 4~6 수준으로 예상된다. 12.0을 그대로 쓰면 표정
강도가 항상 낮게 깎인다.

→ `adapters.PYFEAT_AU_REFERENCE = 5.0`으로 분리했다. **역시 잠정값이며
3번 실측에서 확정한다.**

한 가지 더 — py-feat의 AU 값은 엄밀히는 intensity가 아니라 **검출
확률**이다. 그 합을 표정 강도의 대리값으로 쓰는 것은 근사다. 이 점이
논점 1(네이티브 arousal 사용)에 힘을 실어 주지만, 그건 v2 라이선스가
풀린 뒤의 이야기다.

---

## 출처

- py-feat 2.1.2 배포 소스 직접 확인 — `feat/multitask/__init__.py`,
  `feat/multitask/model_v2.py`, 동봉 `LICENSE`
- [cosanlab/py-feat](https://github.com/cosanlab/py-feat)
- [TadasBaltrusaitis/OpenFace](https://github.com/TadasBaltrusaitis/OpenFace) ·
  [라이선스 전문](https://github.com/TadasBaltrusaitis/OpenFace/blob/master/OpenFace-license.txt) ·
  [상업 라이선스 비용 논의](https://github.com/NumesSanguis/FACSvatar/issues/27)
- [ihp-lab/LibreFace](https://github.com/ihp-lab/LibreFace)
- [MediaPipe Face Landmarker](https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker)
- [av-savchenko/hsemotion-onnx](https://github.com/av-savchenko/hsemotion-onnx)
