# 감정의 기록 (가제) — 컨셉 기획서

마지막 업데이트: 2026-08-23

## 이 파일의 용도
새로운 인터렉티브 작품 아이디어를 구현 가능한 형태로 정리·기록하는 문서. 아직 제목은
가제이며, 아이디어를 계속 얘기하면서 이 파일에 누적 정리해나감. `art-grants-alert`
저장소 자체(공모 알림 스크래퍼)와는 별개 프로젝트이며, 세션이 바뀌어도 대화 맥락을
이어갈 수 있도록 저장해둠. 같은 방식으로 기록 중인 다른 작품: `EMOTIONAL_GEOLOGY_REVIEW.md`
(감정의 지층), `Project_감정의인상.md` (감정의 인상).

## 프로젝트 개요 (초기 — 미정 다수)
- 컨셉 한 줄: 관객/퍼포머의 실시간 캡처 데이터를 각기 다른 공간에서 서로 다른 방식으로
  재해석해 실시간 송출하는 다공간 인터렉티브 작품 (가제 "감정의 기록")
- 전시/시연 장소, 시점, 예산, 공모: 미정
- 공간 규모: 최소 2층 이상 다층 구조 전제 (아래 아이디어 2 참고)

## 비주얼 아이디어 및 컨셉

### 아이디어 1 — 입장 캡처 → 동선 분기 → 공간별 실시간 재해석
- 관객이 처음 입장하면 영상으로 캡처됨
- 이후 정해진 동선을 따라 각각 다른 공간으로 이동
- 각 공간에는 그 관객의 캡처 영상이 공간마다 **서로 다르게 표현·변환**되어 송출됨
  (같은 원본 소스 → 공간별로 다른 알고리즘/필터/스타일 적용)
- **핵심 포인트: 실시간 송출** — 녹화 후 재생이 아니라 캡처와 동시에(또는 최소 지연으로)
  각 공간에 뿌려짐

#### 공간별 매체/표현 방식 (2026-08-23 확정, 08-23 6종으로 확장)
"공간마다 다르게 표현"은 곧 **공간마다 다른 디스플레이·매체**를 쓴다는 뜻으로 구체화됨.
같은 캡처 원본을 매체별로 완전히 다른 형태로 재해석:

| 공간 | 매체 | 표현 방식 |
|---|---|---|
| 1 | LED | TouchDesigner + Kinect로 인물 실루엣 추출해 표현 |
| 2 | PC 모니터 | 웹사이트의 웹페이지로 표현 |
| 3 | 모바일(아이폰/아이패드) | 앱으로 표현 |
| 4 | CRT 모니터 (RGB 케이블) | (표현 방식 논의 필요 — 아래 열린 질문 참고) |
| 5 | 캠코더 디스플레이 | (표현 방식 논의 필요) |
| 6 | VHS 비디오 (구형 TV) | (표현 방식 논의 필요) |

**떠오르는 축**: 1~3이 "동시대 디지털 매체"(LED/웹/앱)라면, 4~6은 "아날로그·구형 방송/영상
매체"(CRT/캠코더/VHS)로 갈라짐 — 같은 감정 데이터가 신형 매체와 구형 매체를 오가며 기록되는
**매체 고고학(media archaeology)** 구조로 읽힘. 제목이 "감정의 **기록**"인 것과도 맞닿음
(VHS·캠코더는 "기록 매체" 자체가 컨셉의 핵심어와 직결).

#### 추가 매체 제안 (2026-08-23, 검토용 — 아직 채택 안 됨)
"기록 매체의 역사"라는 축을 더 밀고 가거나, "화면(디스플레이)"을 넘어 "물리적 흔적"까지
넓힌다면 후보가 될 만한 것들:

- **폴라로이드/인스턴트 필름 프린터** — 화면이 아니라 그 순간 감정의 실물 사진 한 장을
  즉석 인화. 관객이 직접 손에 들고 나갈 수 있는 유일한 "가져가는" 매체가 됨
- **플로터/펜 로봇(예: AxiDraw)** — 감정 데이터를 실시간으로 종이 위에 선으로 그려내는
  "느린 기록". 디지털 데이터가 손으로 그린 듯한 드로잉으로 변환되는 대비
- **감열 영수증 프린터** — 실루엣이 아니라 텍스트/ASCII 아트나 숫자 데이터(심박·표정 수치
  등)로 감정을 "영수증처럼" 출력. 저렴하고 설치가 간단해 다수 설치에도 부담 적음
- **팩스기** — 촬영 즉시 스캔선 방식으로 전송·출력되는 특유의 노이즈/줄무늬 질감. "실시간
  전송+기록"이 동시에 일어나는 매체라 VHS의 "지연" 문제 없이도 아날로그 질감을 낼 수 있음
- **오실로스코프** — 실루엣의 윤곽선이나 움직임을 전자 파형처럼 그려내는 화면. 화면에
  남지만 저장되지 않고 계속 지워지는 "휘발성 기록"이라는 점이 VHS(영구 기록)와 대비됨
- **35mm 필름 슬라이드 프로젝터** — 정지된 한 프레임만 뽑아 필름 질감으로 투사. 영상이
  아니라 "정지된 순간"을 남기는 매체
- **도트매트릭스 전광판(지하철 안내판 스타일)** — 실루엣이 아니라 저해상도 픽셀/텍스트로
  변환해 표시. LED(1번)와는 다른 결의 "디지털"이라 대비 가능

전부 다 쓸 필요는 없고, "화면(라이브 스트리밍형)" 계열과 "실물 출력(테이크아웃형)" 계열을
하나씩만 더해도 컨셉이 풍부해질 수 있음. 특히 폴라로이드나 감열 프린터처럼 관객이 실물을
가지고 나갈 수 있는 매체는 "전시장을 나가도 남는 기록"이라는 의미를 더할 수 있어 보임.

### 아이디어 2 — 라이브 퍼포먼스 → 감정 실루엣 → 다층 공간 다중 송출
- 댄서가 라이브로 퍼포먼스(몸으로 감정을 표현)
- 바로 옆 디스플레이에 그 퍼포먼스가 **감정 실루엣**으로 실시간 변환되어 표현(재생)됨
- 댄서를 캡처하는 카메라 + 알고리즘이 **다양한 화면/포맷**으로 결과를 송출
- 송출되는 공간이 서로 다름 — 예: 2층, 3층, 4층으로 이어지며, **하나의 퍼포먼스 소스가
  여러 공간에 각기 다른 형태로 동시 송출**됨

### 두 아이디어의 공통 구조
- 원본 캡처 소스는 하나(관객 1인 또는 퍼포머 1인)
- 그 소스를 공간마다 다른 알고리즘/표현 방식으로 가공
- 결과물을 여러 공간에 동시에, 실시간으로 분산 송출
- → "하나의 입력이 여러 공간에서 여러 모습으로 동시에 존재한다"는 것이 두 아이디어를
  관통하는 핵심 구조로 보임 (다음 대화에서 확인 필요)

## 열린 질문
1. 아이디어 1(관객 입장 캡처)과 아이디어 2(댄서 라이브 퍼포먼스)는 같은 작품의 두
   구간(존)인가, 아니면 별개의 시나리오/대안 컨셉인가?
2. ~~아이디어 1에서 "공간마다 다르게 표현"— 구체적으로 어떤 축으로 달라지는가?~~
   → **매체(디스플레이 종류)로 확정** (LED 실루엣 / 웹페이지 / 모바일 앱). 단, 새 질문:
   웹페이지·모바일 앱은 그 공간에 **고정 설치된 키오스크**인가, 관객이 **개인 기기로
   접속**하는 개인화 채널(예: QR 접속)인가? 후자라면 "정해진 동선으로 이동"하는 물리적
   공간 개념과 어떻게 공존하는가?
3. "감정 실루엣"의 구체적 형태는? (윤곽선만 남기는 방식 / 파티클 / 열화상 느낌 / 다른 시각화)
4. ~~캡처 방식: 카메라(RGB) 단독인가, 깊이 카메라(Kinect류)나 모션캡처도 포함하는가?~~
   → **LED/실루엣 공간은 Kinect(깊이 카메라) 확정.** 웹페이지·모바일 앱 공간의 캡처
   소스도 동일한 Kinect 원본을 재가공하는 것인지, 별도 카메라가 있는지는 미정.
5. 관객 캡처 → 각 공간 송출까지 허용 지연시간은 몇 초 수준인가?
6. 다층 공간(2~4층) 간 영상 송출은 어떤 방식인가? (건물 내 유선 네트워크 전제? NDI/Spout?
   웹페이지·모바일 앱은 인터넷/사내망을 통한 스트리밍이 필요해짐 — TouchDesigner→LED
   경로와는 기술적으로 완전히 다른 배포 방식)
7. 관객 동선은 자유 관람인가, 순서가 강제되는 시퀀스인가?
8. 퍼포먼스는 상시 라이브인가, 특정 시간대(오프닝 등)에만 진행되는가?
9. 모바일 앱은 네이티브 앱(설치 필요)인가, 웹앱(PWA, 설치 없이 접속)인가?
10. 동시에 여러 관객이 입장/캡처될 경우, 각 매체(6종)에서 "누가 누구의 결과인지"를
    어떻게 구분해 보여주는가? (개인별 세션 ID, QR, 화면 분할 등)
11. **CRT(4)·캠코더(5)·VHS(6)는 실시간 송출이 실제로 가능한가?** — VHS는 태생적으로
    "테이프에 기록 후 재생"이 전제인 매체라 아이디어 1의 "핵심 포인트: 실시간 송출"과
    정면으로 충돌함. 두 가지 해석 가능: (a) 이 세 매체는 실시간이 아니라 **약간의 지연을
    둔 "기록→재생" 구간**으로 의도적으로 설계해 신형 매체(즉시성)와 구형 매체(지연된
    기록)의 시간성 대비를 만드는 것 — 컨셉상 오히려 설득력 있어 보임 (b) 아니면 RGB
    변환 박스로 실시간 신호를 CRT에 뿌리고, VHS도 레코더를 실시간 통과시켜 촬영 순간과
    거의 동시에 재생하는 방식(가능은 하나 장비 구성이 까다로움). 어느 쪽 의도인지 확인 필요.
12. CRT/캠코더/VHS 3종은 각각 어떤 화면 내용을 보여주는가? (LED처럼 실루엣만? 원본 영상
    그대로? 노이즈/트래킹 에러 같은 아날로그 열화를 의도적으로 얹는가?)
13. 6개 매체 모두 하나의 동선(6개 공간)에 대응하는가, 아니면 한 공간에 여러 매체가
    함께 배치되는 "매체 아카이브 존"이 별도로 있는가?

## 다음 단계
- 위 열린 질문에 대한 답을 이어서 정리
- 컨셉이 어느 정도 굳으면 인터랙션 메커니즘 / 비주얼 표현 체계 / 기술 스택 섹션 추가
  (기존 `Project_감정의인상.md` 구조 참고)
